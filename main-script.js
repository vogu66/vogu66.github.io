console.log( "ready!" );

// $("#includedContent").load("b.html");

/*
// code from https://stackoverflow.com/questions/8988855/include-another-html-file-in-a-html-file
function including()
{
	var includes = $('[data-include]');
	jQuery.each(includes, function()
	{
		console.log('in');
		var file = $(this).data('include') + '.included';
		$(this).load(file);
	});
}

including();*/
